## Backend Setup

1. Install dependencies:

```
npm install
```

2. Provide environment variables (create a `.env` file at the repo root):

```
PORT=5000
CLIENT_URL=http://localhost:3000
POSTGRES_URI=postgres://username:password@localhost:5432/dbname
```

3. Run the dev server:

```
npm run dev
```

